#include <stdio.h>

int main () {
    int person[4][3]; 
    int sum[3];
    float ratio[3];
    int i, j;
    // 4개 층별 3개 호실의 점호 인원 수
    // 점호 인원 수 합계 (남, 여, 전체) 
    // 점호 인원 수 비율 (남, 여, 전체)
    // 반복문을 위한 변수




    for (i = 1; i < 5; i++) {
        if (i == 1 || i == 3){
            printf("Check Room (%s)\n", (i == 1) ? "MEN" : "WOMEN");
        }
        for (j = 1; j < 4; j++){
            printf("Room #%d0%d > ", i, j);
            scanf(%d)
        }
    }

    printf("Check Result");












    return 0;
}